# Contributing to ZZZ-OS

这份指南的目标很简单：让新贡献者能尽快在本仓库里做出正确、容易合并的修改。

## 提交前检查

提交前请尽量完成下面这些最小验证：

- 能成功编译你修改涉及的目标架构
- 如果改了启动、调度、内存管理、系统调用或文件系统，至少完成一次实际启动验证
- 如果改动影响使用方式、目录结构或开发流程，请同步更新文档

如果你没法完成其中某一项，也没关系，请在提交说明里明确写出来。

## 代码组织约定

### 通用层

下面这些目录默认是通用层：

- `kernel/`
- `mm/`
- `fs/`
- `drivers/core/`
- `drivers/of/`
- `include/os/`
- `include/mm/`
- `include/fs/`

除非是修正真实的通用层问题，否则请不要为了兼容某个架构的历史实现去修改这些目录中的接口。

### 架构层

架构相关差异应优先落在：

- `arch/arm/`
- `arch/riscv64/`
- `arch/<arch>/include/asm/`
- `arch/<arch>/mm/`
- `arch/<arch>/kernel/`
- `arch/<arch>/boot/`

一个简单判断标准是：如果某段代码只因为寄存器、异常模型、页表格式、上下文切换或启动流程不同而存在，它大概率应该属于架构层。

### 驱动层

驱动请尽量复用现有框架：

- 设备发现优先基于 Device Tree
- 中断接入优先走现有 IRQ 子系统
- 控制器与外设驱动优先挂到已有 bus / platform 模型

如果你新增驱动，请同时检查：

- DTS 是否需要补节点或属性
- `objs.build` 是否已纳入构建
- `defconfig` 是否应启用对应配置

## 提交风格建议

推荐一个提交只做一件事，例如：

- 一个 bug fix
- 一个小型重构
- 一个独立驱动
- 一组彼此紧密相关的文档更新

请尽量避免把这些内容混在同一个提交里：

- 架构重构
- 用户态功能变更
- 驱动新增
- 纯格式化或大面积无语义改动

## Pull Request 建议写法

一个容易评审的 PR，最好能说明：

- 改了什么
- 为什么要这样改
- 影响哪些架构或子系统
- 你做了哪些验证
- 还有哪些已知限制或后续工作

如果是 RISC-V64 对齐 ARM 的重构，建议明确写出：

- 对齐的是哪一个 ARM 接口或行为
- 删除了哪些旧的 RISC-V64 历史实现
- 有没有碰到暂时无法统一的差异点

## 文档约定

下面这些文件是对新贡献者最重要的入口：

- [README.md](/home/wqqqq/ZZZ-OS/README.md)
- [doc/README.md](/home/wqqqq/ZZZ-OS/doc/README.md)
- [doc/build.md](/home/wqqqq/ZZZ-OS/doc/build.md)

如果你改了以下内容，请考虑同步补文档：

- 构建流程
- 启动流程
- 子系统接口
- 用户态可见行为
- DTS 或硬件支持矩阵

## 当前没有正式声明的事项

仓库目前没有统一的顶层许可证文件，也没有正式 CI。提交代码默认表示你愿意在项目现有维护方式下协作，但如果你的单位或组织对授权、归属、审计有要求，请先联系维护者确认。
