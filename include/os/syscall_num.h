#ifndef SYSCALL_NUM_H
#define SYSCALL_NUM_H

/*
 * Compatibility include. Syscall numbers are architecture ABI, so the real
 * table lives under arch/<arch>/include/asm/syscall_num.h.
 */
#include <asm/syscall_num.h>

#endif
