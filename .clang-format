# 基本配置
BasedOnStyle: LLVM       # 基于哪个预设风格修改
IndentWidth: 4           # 缩进宽度（空格数）
TabWidth: 4              # Tab 宽度
UseTab: Never            # 不使用 Tab，用空格代替（可选：ForIndentation 仅缩进用 Tab）

# 大括号格式
BraceWrapping:
  AfterClass: true       # 类定义后换行（如 class A { → 换行）
  AfterFunction: true    # 函数定义后换行
  AfterControlStatement: true  # 控制语句（if/for 等）后不换行

# 括号内缩进
IndentWrappedFunctionNames: false  # 函数名换行时不缩进
ContinuationIndentWidth: 4         # 换行后的续行缩进宽度

# 空格规则
SpaceAfterCStyleCast: false  # C 风格强制转换后加空格（如 (int) x）
SpacesInParentheses: false  # 括号内不加空格（如 (a + b) 而非 ( a + b )）
SpacesInSquareBrackets: false # 方括号内不加空格

# 换行规则
ColumnLimit: 0         # 每行最大字符数（超过自动换行）
AllowShortFunctionsOnASingleLine: Inline  # 短函数是否单行（Inline 仅内联函数）